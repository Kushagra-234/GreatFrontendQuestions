// fetch is cached by default leading to static rendering
// cache no store se ssr

// useSearchParams used for query params readings

// page route hsi jo change hota hai on render and layout nhi hota change on render
// it persists along render

// middleware  ek funciton jo reuqest ke flow ke beech me use hota hai
// page ya api hit hone se pehle
// Route Groups help organize routes and apply different layouts without affecting the URL structure.”

// Parallel routes allow multiple independent routes to render simultaneously inside the same layout without replacing each other’s UI.

// Same layout me multiple independent pages/routes with their own navigation + loading + error + URL state” chahiye ho, tab parallel routes justify hote hain.
