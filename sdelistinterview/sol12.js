// event delegation ek aisi process hai jisme instead of attaching event listeners to child we attach listeneres to parent and handle it using event bubbling 

// hota kya  hai maan lo 100 child hai to 100 event listner lgaoge kya we use only 1 and attach it with parent and handle it using event bubbling 

// child pe click hua -> event propogates to parent -> parent ka event listener trigger hota hai