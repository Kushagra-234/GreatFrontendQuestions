Event loop kaise kaam karta hai? Microtask vs macrotask?


event loop is a pehenoemon which decides what should be pushed in call stack when it is empty.as js is single threaded language so it handles asynchronous tasks ina a different way
microtask queue and macrotask queue are used to handle asynchronous tasks,microtask queue is used for promises and macrotask queue is used for web APIs and events , microtask has higher priority than macrotask





a protoype is a way in which one obejct can access property of another object 
so basically if object a is protoype of b than a can access properties if a 

prootype chainign is a mechanism in which a object looks for a property in itslef first and then its prototype and then its prototypes prototypes
thus forming a chain like structure known as protype cjhain 


protype is a method in constructor function and _proto__ is a method on object which points to its prototype 