// array ko flat krta hai flat() function and flatMap() function me ye hota hai pehle map and then flat 

const arr=[1,2,[3,4]]

arr.flat()
a




// requestAnimationFrame is synced with the browser’s rendering cycle, while setTimeout(0) is a macrotask with an unpredictable delay.


// requestAnimationFrame(() => {
//   console.log("frame");
// });
// Core difference (1 line) 🎯
// setTimeout(0) → “jab mil jaaye tab chala do”

// requestAnimationFrame → “next repaint se pehle chala do”


