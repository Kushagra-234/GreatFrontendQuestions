// virtualization is a process in which we only render things which are visible on screen
// Semantic tags are HTML tags that clearly describe the meaning of their content to browsers, search engines, and screen readers. They improve accessibility, SEO, and code readability.

// Examples are <header>, <nav>, <article>, <section>, <footer>.
// They help screen readers understand page structure and help search engines index content better.

// position: relative se element apni normal jagah par rehta hai, lekin top, right, bottom, left se offset ho sakta hai bina dusre elements ko affect kiye. Yeh absolute children ke liye container banata hai bina apni jagah badle. Space original rahti hai, sirf visual shift hota hai.
// ​
// ​

// Absolute Position
// position: absolute element ko normal flow se nikaal deta hai aur nearest relative (non-static) parent ke mutabik position karta hai, warna viewport ke liye. Dusre elements iski jagah le lete hain kyunki yeh overlap karta hai.
// ​
// ​


// jyadatar relative parents and absolute children use karte hain.
// apni jagah pe rhta hai pr right left se upar neeche hota and absolute me wo nearest relative parent ke hissab se mpve hota hai aisa hai na  ]
// Flexbox is one-dimensional layout system, Grid is two-dimensional layout system.